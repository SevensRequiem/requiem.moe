const favframeCount=70;const frameTime=100;const frameLocation='/static/favicons/ramen/';const frames=[];for(let i=0;i<favframeCount;i++){frames.push(`${frameLocation}${i}.gif`)}
let currentFrame=0;const favicon=document.querySelector('link[rel="icon"]');favicon.href=frames[currentFrame];setInterval(()=>{currentFrame=(currentFrame+1)%favframeCount;favicon.href=frames[currentFrame]},frameTime)